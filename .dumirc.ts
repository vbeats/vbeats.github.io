import { defineConfig } from 'dumi';

export default defineConfig({
  base: '/',
  publicPath: '/',
  themeConfig: {
    name: 'vlog',
    footer: 'Powered by vbeats'
  },
  analytics: {
    ga_v2: 'G-ERE4E7HP60',
  }
});
